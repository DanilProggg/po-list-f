import { OffsetTimePipe } from './offset-time.pipe';

describe('OffsetTimePipe', () => {
  it('create an instance', () => {
    const pipe = new OffsetTimePipe();
    expect(pipe).toBeTruthy();
  });
});
